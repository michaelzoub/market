/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['community.akamai.steamstatic.com'], // Add any other domains if needed
      },
};

export default nextConfig;
