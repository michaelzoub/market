/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['community.akamai.steamstatic.com', 'cryptologos.cc', 'upload.wikimedia.org'], // Add any other domains if needed
      },
};

export default nextConfig;
